# 2026-06-11 开发日志

## 完成事项

### 1. 帖子详情页重设计
- **布局**：从纵向单栏改为横向分栏——帖子内容区70% + 评论区30%
- **参考**：`fyp/code.html` 融合版设计
- **CSS**：新增 `explore-detail-layout`、`post-section`、`comment-section`、评论列表、inline-reply-box、楼中楼toggle、底部输入栏等约250行样式
- **移动端**：≤767px 改为上下布局

### 2. 新版评论系统
- **评论渲染**：avatar、昵称、时间、内容、图片
- **交互**：
  - 主输入框固定底部（input + 图片 + 发送）
  - 行内回复：点击"回复"展开 inline-reply-box（input + 图片 + 发送）
  - 楼中楼：默认隐藏，底部"展开 N 条回复" toggle
  - 点赞：localStorage 记录，♡/♥ 切换
  - 删除：弹窗确认后删除
- **图标**：Remix Icon（`ri-heart-line/fill`、`ri-chat-3-line`、`ri-delete-bin-line`、`ri-image-line`）
- **简化**：移除了旧版冷却计时器系统（~400行）、forum-reply-row 渲染、exploreExpandedReplies 状态管理等

### 3. 探索页修复
- 发布FAB → 导航到 `explore-tutorial` 而非直接编辑器
- 教程页改为独立路由页面，左上角返回箭头

### 4. 卡片系统修复
- 卡片编辑modal：MD3输入框 + days_matter日期选择器
- 编辑器卡片颜色：仅图标着色，不再全背景
- 右侧模板列表：图标着色
- 复制按钮：圆角8px + hover背景
- 移除卡片左侧border-left阴影
- 卡片插入后前后加 `<div><br></div>` 确保可继续输入

### 5. 富文本编辑器增强
- 工具栏新增：删除线、有序/无序列表、左/中/右对齐、插入链接
- 链接改用自定义modal（MD3输入框 + 显示文字）
- 字符计数
- 键盘快捷键：Ctrl+B/I/U/K
- serializeEditor 改为保留HTML格式

### 6. 后端适配
- `explore_posts.js`：content 改为 JSON 数组存取；列表API增加 creator_id 过滤；文字摘要strip HTML标签
- `explore_comments.js`：支持图片上传 + 楼中楼 + 硬删除

## 文件变更
| 文件 | 变更 |
|------|------|
| `public/js/pages/explore.js` | 完全重写（1020行→约500行），新布局+评论系统 |
| `public/css/style.css` | +约500行（分栏布局、评论区、卡片颜色、编辑器样式） |
| `public/js/pages/post-editor.js` | 完全重写（左右分栏、contentEditable、卡片嵌入） |
| `public/js/components/card-renderer.js` | 新增 data-template、renderPostCard适配新API |
| `routes/explore_posts.js` | content JSON存取、creator_id过滤 |
| `routes/explore_comments.js` | 图片上传、楼中楼、硬删除 |
| `server.js` | 新增7张表+模板预置+路由挂载 |

### 7. 帖子详情页修复（第二轮）
- **板块化**：帖子区和评论区各加圆角白色卡片底板（`border-radius + border + background`）
- **用户头像**：帖子详情标题上方增加作者头像行（36px圆形头像 + 昵称 + 时间）
- **评论图片**：主评论和行内回复均改用 FormData fetch 提交，支持图片上传
- **回复平铺**：楼中楼改为平铺 "A 回复 B: content" 格式，所有子回复递归展平为一维列表
- **按钮文字化**：回复/删除按钮改为纯文字（移除图标）
- **点赞靠右**：点赞按钮用 `margin-left: auto` 推到右侧
- **inline-reply 对齐**：加 `min-width:0` + `align-items:center`
- **时间修复**：`formatTimeAgo` 补上 `+08:00` 时区标记
- **RTE 编辑还原**：`restoreBlocksToCanvas` 改为 HTML → `div.innerHTML`，保留格式

## 待办
- 图片多图支持
